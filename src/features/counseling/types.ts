// 운영진 "가능 시간 등록" 탭에서 쓰는 09:00~19:00 30분 단위 슬롯 1개 - 09:00~19:00 전체 고정 시간에
// 서버가 내려준 열림/예약 상태를 합쳐서 만든다(닫힌 시간은 서버 응답에 아예 없어서 별도로 채움)
export type CounselingTimeSlot = {
   time: string; // '09:00' 형식
   // 운영진이 상담 가능하다고 켠 시간인지
   isOpen: boolean;
   // 학생이 이미 신청해서 예약된 시간인지 - true면 항상 켜진 채로 토글이 막힌다(끌 수 없음)
   isBooked: boolean;
};
